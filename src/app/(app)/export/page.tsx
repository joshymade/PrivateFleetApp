export default function ExportPage() {
  return (
    <main className="mx-auto w-full max-w-lg p-6">
      <h1 className="text-2xl font-semibold tracking-tight">Canvas export</h1>
      <p className="mt-2 text-sm text-zinc-600">
        Composite JPG export with metadata strip — UI coming later.
      </p>
    </main>
  );
}
